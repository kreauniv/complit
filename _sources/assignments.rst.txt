Programming problems
====================

Things to do with strings
-------------------------

1. Split a string into lines. (simple)
2. Split text into sentences. (simple+)
3. Get a list of words in a string. (involved)
4. Find where a particular substring occurs in a given string. (simple)
5. Find all occurrences of a substring in a given string. (involved)
6. Given a string containing brackets, check that the brackets match correctly. (involved)
7. Extract hashtags from a string. (involved)
8. Find email addresses in a string (complex)
9. Find all URLs in a string (complex)
10. Automatically hyperlink all URLs in a string by translating the string to HTML. (complex, but close to 9)
11. Given a string containing HTML, check that the tags are properly structured. (complex)
12. Turn a HTML string into a "tree". (advanced)
13. Convert a number given in digit representation into a numeric value (involved)
14. Extract list of all numbers in the given string (involved)
15. Convert a string into title case (simple+)
16. Convert a string into upper/lower case (simple)
17. Check if a given string is a valid identifier in a programming language of your choice. (involved)
18. Split a string into parts separated by a given string. (involved)
19. Join a string given parts and a separator (simple)
20. Count number of times each word in a string occurs. (complex)

Mathy stuff
-----------

1. Number guessing game (simple)
2. Compute approximate square root (simple) using :math:`x_{n+1} = \frac{1}{2}(x_n + \frac{a}{x_n})`.
3. Generalize (2) to n-th root. (involved)
4. Model a rocket launch. (advanced)
5. Draw the paths of a double pendulum. (advanced)
6. Given a function, plot as a graph using 2htdp/image (involved)

Language
--------

Sound and music
---------------

1. Produce a sine tone.


Solutions
---------

Split a string into lines
~~~~~~~~~~~~~~~~~~~~~~~~~

.. code-block:: racket

    (define (list-of-lines-in-string str)
        (if (empty-string? str)
            empty
            (cons (first-line str) (rest-of-the-lines str))))

    (define (empty-string? str)
        (= 0 (string-length str)))

    (define (first-line str)
        (substring str 0 (first-newline-character-position str)))

    (define (rest-of-the-lines str)
      (if (>= (first-newline-character-position str) (string-length str))
          empty
          (list-of-lines-in-string (substring str (+ 1 (first-newline-character-position str))))))

    (define (first-newline-character-position str)
        (next-newline-character-position-starting-from str 0))

    (define (next-newline-character-position-starting-from str i)
      (if (position-beyond-end-of-string str i)
          (string-length str)
          (if (equal? (string-ref str i) #\newline)
              i
              (next-newline-character-position-starting-from str (+ i 1)))))

    (define (position-beyond-end-of-string str i)
      (>= i (string-length str)))

Find where a given substring occurs in a string
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Naive start - 

.. code-block:: racket

    (define (substring-position str substr)
        (if (substring-at-start? str substr)
            0
            (+ 1 (substring-position (substring str 1) substr))))

    (define (substring-at-start? str substr)
        (equal? (substring str 0 (string-length substr)) substr))


.. code-block:: racket

   (define (substring-position str substr)
       (if (substring-can-be-found? str substr)
           (if (substring-at-start? str substr)
               0
               (next-pos-if-valid (substring-position (substring str 1) substr)))
           #f))

   (define (substring-at-start? str substr)
       (equal? (substring str 0 (string-length substr)) substr))

   (define (substring-can-be-found? str substr)
       (<= (string-length substr) (string-length str)))

   (define (next-pos-if-valid p)
       (if p (+ p 1) #f)) ; Also expressible as (and p (+ p 1))
            
All occurrences of a substring
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Naive version

.. code-block:: racket

   (define (substring-positions str substr)
      (let* ([p (substring-position str substr)]
             [pend (+ p (string-length substr))])
          (cons p
                (offset-by pend (substring-positions (substring str pend) substr)))))

   (define (offset-by dpos list-of-positions)
        (map (lambda (x) (+ x dpos)) list-of-positions))

.. code-block:: racket

   (define (substring-positions str substr)
      (let ([p (substring-position str substr)])
         (if p
             (let ([pend (+ p (string-length substr))])
                (cons p
                      (offset-by pend (substring-positions (substring str pend) substr))))
             empty)))

   (define (offset-by dpos list-of-positions)
        (map (lambda (x) (+ x dpos)) list-of-positions))


